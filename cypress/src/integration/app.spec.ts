import { getSelect } from '../support/app.po';

describe('ngx-component-harnesses', () => {
  beforeEach(() => cy.visit('/'));

  it('should display your favorite ocean creature in a sentence', () => {});
});
